export const npm = {
  logLevel: 'silent',
  progress: false
};

export const defaultProject = 'https://github.com/katallaxie/angular2-preboot'; // I know, that is a bit selfish ;-).

export default {
  npm,
  defaultProject
}
